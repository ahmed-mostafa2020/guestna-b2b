import { Typography } from "@mui/material";
import React from "react";

const OrderDataLabel = ({ label }) => {
  return <Typography className="!font-somar !mb-2 ">{label}</Typography>;
};

export default OrderDataLabel;
